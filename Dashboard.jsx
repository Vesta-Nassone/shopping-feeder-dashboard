import React from "react";

export default function Dashboard() {
    return (
        /* this component will hold the top two rows of InfoCards and the Graphs */
		<div>
			<h1>This is the dashboard component</h1>
		</div>
	);
}
